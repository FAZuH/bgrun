name: Release & Deploy

on:
  push:
    branches:
      - release

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Release
    uses: ./.github/workflows/_release.yml
    with:
      binary-name: bgrun
      platforms: linux
      build-binaries: true
      publish-crates: false
    secrets: inherit
